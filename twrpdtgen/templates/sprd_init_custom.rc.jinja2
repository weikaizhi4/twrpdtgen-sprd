# Keep recovery permissive after the stock policy is patched at build time.
on init
    # Use inits builtin so this works before /system/bin is available and
    # does not depend on a recovery-domain transition for setenforce.
    setenforce 0
